// import { HOMEPAGE, PERKSPAGE, SCHEDPAGE, TESMPAGE, TIPSPAGE } from "./actionsTypes";

/* const initialState = {
    pageVal: 0
};
*/

/* export const menuReducer = (state = initialState, action) => {
    switch (action.type) {
        case HOMEPAGE:
            return {
                ...state,
                pageVal: 0
            };
        default: return state;
    }
}
*/